<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="20dp"
    android:background="#0D1117">

    <TextView
        android:id="@+id/tv_breathing_phase"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/mindfulness_measuring_hrv"
        android:textColor="#AACCFF"
        android:textSize="18sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="16dp" />

    <!-- Circular-style progress (horizontal styled round) -->
    <ProgressBar
        android:id="@+id/progress_breathing"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="160dp"
        android:layout_height="160dp"
        android:progress="0"
        android:max="10"
        android:progressTint="#4488FF"
        android:progressBackgroundTint="#1A2A44"
        android:layout_marginBottom="8dp" />

    <TextView
        android:id="@+id/tv_breathing_countdown"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="--"
        android:textColor="#FFFFFF"
        android:textSize="40sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="16dp" />

    <TextView
        android:id="@+id/tv_hrv_score"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="HRV -- ms"
        android:textColor="#66BBFF"
        android:textSize="14sp"
        android:gravity="center"
        android:layout_marginBottom="24dp" />

    <Button
        android:id="@+id/btn_mindfulness_stop"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="@string/mindfulness_stop"
        android:backgroundTint="#1A2A44"
        android:textColor="#AACCFF" />

</LinearLayout>
