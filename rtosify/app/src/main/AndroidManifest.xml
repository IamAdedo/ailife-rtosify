<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ============================================================ -->
    <!-- HARDWARE FEATURES (ChromeOS compatibility) -->
    <!-- ============================================================ -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <!-- ============================================================ -->
    <!-- PERMISSÕES -->
    <!-- ============================================================ -->

    <!-- Bluetooth (Legado - Android 11 e inferior) -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <!-- CORREÇÃO: Localização Precisa necessária para ler SSID do Wi-Fi em TODAS as versões -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Opcional: Localização em Background para Android 10+ (melhora leitura de Wi-Fi com tela desligada) -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Bluetooth (Moderno - Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" tools:targetApi="s" />

    <!-- Serviços e Sistema -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" /> <!-- Necessário para ler Wi-Fi no serviço -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" tools:targetApi="34" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Gerenciamento de Apps e Arquivos -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- NOVAS PERMISSÕES PARA STATUS -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Leitura de Arquivos (APK) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    
    <!-- WiFi (Android 13+) -->
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" android:usesPermissionFlags="neverForLocation" tools:targetApi="tiramisu" />
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />

    <queries>
        <package android:name="moe.shizuku.privileged.api" />
    </queries>

    <!-- ============================================================ -->
    <!-- APLICAÇÃO -->
    <!-- ============================================================ -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Smartwatch"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true">

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false" />

        <!-- Activity Principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AboutActivity"
            android:label="About"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ShareToWatchActivity"
            android:exported="true"
            android:label="@string/automation_sharing_sync_title"
            android:theme="@style/Theme.Smartwatch">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>

        <activity
            android:name=".WiFiPairingActivity"
            android:exported="false"
            android:theme="@style/Theme.Smartwatch" />

        <activity
            android:name=".CameraActivity"
            android:exported="false"
            android:theme="@style/Theme.Smartwatch" />

        <activity
            android:name=".WelcomeActivity"
            android:exported="false"
            android:theme="@style/Theme.Smartwatch" />

        <!-- Force QR Scanner to Portrait -->
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="portrait"
            tools:replace="android:screenOrientation"
            android:stateNotNeeded="true" />

        <!-- Lista de Apps (Watch) -->
        <activity
            android:name=".AppListActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Smartwatch" />

        <!-- Configurações de Notificação -->
        <activity
            android:name=".NavigationSettingsActivity"
            android:label="@string/nav_settings_title"
            android:parentActivityName=".NotificationSettingsActivity"
            android:theme="@style/Theme.Smartwatch" />
        <activity
            android:name=".NotificationSettingsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Smartwatch" />

        <!-- Lista de Apps para Notificações -->
        <activity
            android:name=".NotificationAppListActivity"
            android:exported="false"
            android:parentActivityName=".NotificationSettingsActivity"
            android:theme="@style/Theme.Smartwatch" />

        <activity
            android:name=".AppNotificationSettingsActivity"
            android:label="@string/notif_settings_title"
            android:parentActivityName=".NotificationAppListActivity"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Smartwatch" />
        <!-- Regras de Notificação -->
        <activity
            android:name=".FileObserverSettingsActivity"
            android:label="File Observer Rules"
            android:parentActivityName=".NotificationSettingsActivity"
            android:theme="@style/Theme.Smartwatch" />

        
        <activity
            android:name=".DynamicIslandSettingsActivity"
            android:theme="@style/Theme.Smartwatch" />

        <activity
            android:name=".AndroidNotificationSettingsActivity"
            android:parentActivityName=".NotificationSettingsActivity"
            android:theme="@style/Theme.Smartwatch" />
            
        <activity
            android:name=".DynamicIslandBackgroundActivity"
            android:label="@string/di_background_title"
            android:parentActivityName=".DynamicIslandSettingsActivity"
            android:theme="@style/Theme.Smartwatch" />
        
        <!-- Dynamic Island App Blacklist -->
        <activity
            android:name=".DIBlacklistActivity"
            android:exported="false"
            android:parentActivityName=".DynamicIslandSettingsActivity"
            android:theme="@style/Theme.Smartwatch" />
            
        <activity
            android:name=".AppPickerActivity"
            android:exported="false"
            android:parentActivityName=".AppListActivity"
            android:theme="@style/Theme.Smartwatch" />

        <activity
            android:name=".FileManagerActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Smartwatch" />

        <activity
            android:name=".FindDeviceActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Smartwatch" />

        <activity
            android:name=".FindDeviceAlarmActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Smartwatch" />
            
        <activity
            android:name=".PermissionActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Smartwatch" />

        <activity
            android:name=".DirectoryPickerActivity"
            android:exported="false"
            android:theme="@style/Theme.Smartwatch" />

        <activity
            android:name=".WatchAutomationsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Smartwatch" />

        <activity
            android:name=".HealthDetailActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Smartwatch" />

        <activity
            android:name=".watchface.WatchFaceActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Smartwatch" />

        <activity
            android:name=".BatteryDetailActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Smartwatch" />

        <activity
            android:name=".AlarmManagementActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Smartwatch" />

        <activity
            android:name=".TerminalActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Smartwatch" />

        <activity
            android:name=".MirrorActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Smartwatch" />

        <activity
            android:name=".MirrorSettingsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Smartwatch" />

        <activity
            android:name=".NetworkSettingsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Smartwatch" />

        <activity
            android:name=".InternetTestActivity"
            android:exported="false"
            android:parentActivityName=".NetworkSettingsActivity"
            android:theme="@style/Theme.Smartwatch" />

        <!-- Serviço Bluetooth -->
        <!-- Adicionado "location" para permitir leitura de Wi-Fi em background -->
        <service
            android:name=".BluetoothService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|location" />

        <!-- Accessibility Service for Clipboard Access -->
        <service
            android:name=".RtosifyAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true"
            android:process=":accessibility">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name=".MirroringService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaProjection"
            tools:targetApi="q" />

        <!-- Listener de Notificações -->
        <service
            android:name=".MyNotificationListener"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Boot Receiver -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    <!-- Notification Action Receiver -->
        <receiver
            android:name=".NotificationActionReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Widget Configuration -->
        <activity
            android:name=".widget.WidgetConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.Smartwatch">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- Status Widget -->
        <receiver
            android:name=".widget.StatusWidget"
            android:label="@string/widget_status_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.ailife.rtosify.widget.ACTION_WIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_status_info" />
        </receiver>

        <!-- Health Widget -->
        <receiver
            android:name=".widget.HealthWidget"
            android:label="@string/widget_health_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.ailife.rtosify.widget.ACTION_WIDGET_HEALTH_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_health_info" />
        </receiver>

        <!-- FileProvider (OBRIGATÓRIO PARA INSTALAÇÃO) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>